domain: inqysitor.github.io       # if you want to force HTTPS, specify the domain without the http at the start, e.g. example.com
url: https://inqysitor.github.io  # the base hostname and protocol for your site, e.g. http://example.com
baseurl: /progress_knight_2/      # place folder name if the site is served in a subfolder